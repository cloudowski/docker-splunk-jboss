There are few environments that controls how Splunk is configured during run

SPLUNK_PWD - splunk password (admin by default)
SPLUNK_ENDPOINT - change web access endpoint (/ by default)
SSH_PUBKEY - ssh public key to include in root's authorized_keys file


